"""Python mirror of the shared scene-graph schema.

See `models.py`. The canonical schema is `shared/types/scene.ts`; this package must
conform to it, and `shared/fixtures/` holds the golden documents that prove it does.
"""
