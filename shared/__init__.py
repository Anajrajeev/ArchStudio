"""Shared cross-language definitions for ArchStudio.

Only `shared.python` is importable from Python; `shared/types/` and `shared/geometry/`
are TypeScript and `shared/fixtures/` is data consumed by both sides.
"""
